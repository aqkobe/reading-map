# 儿童阅读成长地图 · 改动说明（Changelog / Plan）

> 单文件离线优先的儿童阅读成长应用（HTML/CSS/JS 一体，无构建步骤）。
> 数据优先存浏览器本地（IndexedDB，localStorage 兜底），可选接入 Supabase 做家庭多设备共享。
> 本说明随 `儿童阅读成长地图.html` / `index.html` 同步更新，记录每一轮的需求与落地情况。

---

## 当前版本（最新）包含的改动

### 0. 修复：多设备数据不同步 + 重新进入仍弹出「选择小读者」
- **Bug A（同码不同步）根因**：`doPush` 是把**本机全量状态** `upsert` 覆盖云端整行。当 B 端推送时，会用「只有 B 的数据」覆盖云端，把 A 端已加的书/打卡/评论**抹掉**；`mergeRemote` 又只按 `id` 新增、不更新已有记录，于是评论与进度修改也传不过去。
- **Bug A 修复（读-改-写）**：
  1. `doPush` 改为**先读云端 → `mergeRemote` 合并进本机 → 再写回并集**，保证多端写入时云端始终保留「所有人数据的并集」，彼此不再互相覆盖。
  2. `mergeRemote` 升级为**按 `id` + `updatedAt` 冲突裁决（最后写入者胜出）**，新增与修改（书名/评论/进度/状态）都能同步；小读者资料同样按 `updatedAt` 合并。
  3. 书籍、打卡、资料新建/保存/读完时写入 `updatedAt`，作为冲突时间戳。
  4. 新增 **15 秒定时轮询拉取**作为 Supabase Realtime 未开启该表时的兜底，保证不靠刷新也能持续同步。
- **Bug B（重新弹出设置）根因**：`loadState` 的 2 秒 `Promise.race` 在网页端可能误判为「无数据」返回 `null`，而 `init` 里无条件 `save()` 会把它**当空状态写回**，把已存资料清空 → 下次进入又是空 → 永远重弹；且 `init` 在**拉取云端之前**就判断 `setup`。
- **Bug B 修复**：`loadState` 仅在 `file:` 协议下保留 2 秒兜底，网页端直接等待；`init` 改为**先 `startSync()` 拉云端**再判断 `setup`，且仅有「确有已加载数据」时才落稳，避免空覆盖。
- **验证**：双设备仿真 6 项全 PASS——A 加书 X → B 拉到 X；B 加书 Y 推送后云端仍保留 X+Y（旧逻辑此处会丢 X）；A 拉到 X+Y；冲突编辑按 `updatedAt` 正确合并；B 设置档案后 A 拉到 `setup=true`（不再重弹）。

### 0.2 修复：书架删除书籍不能同步到其他设备
- **根因**：read-merge-write 只按 `id` **增/改**记录，从不移除；A 删书后本地没了，但云端那本书还在，`doPush` 读云端合并时又把该书加回本机，删除自然传不到 B。
- **修复（删除墓碑 tombstone）**：
  1. `state` 新增 `deleted:{books:{},checkins:{}}`，删除时记录 `id → 时间戳` 墓碑。
  2. `delBook` 写入该书墓碑并移除；**该书关联的打卡记录一并墓碑 + 移除**（删除书籍不再残留阅读记录）。
  3. `mergeRemote` 先合并双方墓碑（取并集），再在合并书籍/打卡时**跳过并过滤掉墓碑里的 `id`**，两端拉取即真正删除。
  4. `doPush` 的 `row.data` 携带 `deleted`，墓碑随同步传播。
- **验证**：双设备仿真 6 项全 PASS——A 加 X/Y → B 拉到；A 删 X（本地只剩 Y、墓碑记 X）；A 推送后云端 X 被移除（不再被复活）；B 拉取 X 消失；B 删 Y 连带打卡 cY；A 拉取后书与打卡全清空。

### 0.1 修复：自定义头像（本地照片）在家庭共享中不被同步
- **根因**：`mergeRemote` 对 `child` 做的是 `state.child = rd.child` 整体覆盖（最后写入者胜出）。当另一台使用表情头像（无照片）的设备后推送时，会把云端已设好的照片覆盖成 `null`；而本机拉取到 `null` 又会清掉本地照片，导致「照片不共享」。
- **修复**：
  1. `mergeRemote` 改为字段合并——远程 `child` 没有照片但本机有，则**保留本机照片**（拉取不再抹掉共享头像）。
  2. `doPush` 增加保护——本机没有照片且**非用户主动清除**时，先读取云端已有照片并沿用再推送，无照片的设备永远不会把家庭共享头像清掉。
  3. 「切回表情头像」`clearPhoto` 标记 `explicitClearPhoto`，确保用户**主动**移除头像时仍能正常推 `null` 生效。
- **验证**：双设备仿真 4 项用例全 PASS——A 设照片后云端有照片；B 以表情头像拉取后本地仍保留照片；B 无照片推送时云端照片不被清；主动清除时云端照片被正确移除。

### 1. 清空数据：按钮变红 + 真正清掉所有数据
- **UI**：设置卡片里的「🗑️ 清空全部数据」按钮改为**实心红色**（红渐变 + 白字 + 整行宽度），与二次确认弹窗里的「确定清空」红色风格一致。
- **逻辑强化**：旧的清空只 `state=seed(); save()`，而 `save()` 内的云端推送会因 `familyCode` 已被置空而提前返回，导致**云端家庭共享记录清不掉、下次拉取还会恢复**。
- **新逻辑**：内存置空 → 落空本地 IndexedDB/localStorage → 额外 `clear()` 本地语音库 `audio` store → 用原 `familyCode` 把空状态 `upsert` 推到 Supabase → 再清空 `familyCode`。
- **验证**：仿真确认清空后本地 0 书 / 0 打卡 / `familyCode=null` / 0 条语音；云端家庭记录归零（0 书 / 0 打卡 / 空孩子名）。

### 2. 打卡与书籍绑定（一次只追一本）
- 打卡弹窗的「本次读的是哪本书」下拉框：
  - **存在「正在读」的书** → 只显示这一本（标签「📖 正在读的书（本次只能为它打卡）」），不可选其他书；
  - **没有正在读的书** → 列出全部书供挑选（按 正在读→想读→已读完 排序并带状态标签）。
- 新增书籍 `总页数` **默认 10 页**，且为**必填**（未填或 ≤0 会拦截并提示）。

### 3. 页码记录与自动读完
- 「读到第几页」动态加 `max=总页数`，输入超界自动截断；「本次读了多少页」不超过剩余页数。
- 若 `读到第几页 ≥ 总页数`，该书**自动标记已读完**（含 `finishedAt`）。
- 打卡弹窗新增勾选框「📕 这次把这本书读完啦（不必填页码）」，勾选后隐藏页码输入，提交即标记完成。
- 书籍详情页（点书架里的书）新增「✅ 全书阅读完毕」按钮（未读完时显示），一键完成、无需记页码。

### 4. 书籍详情时间线
- 点书架中的书打开详情面板：展示封面、状态、**进度条百分比**、累计时长。
- 下方用**时间线**展示该书所有打卡记录（日期、阅读分钟、页码、评论、语音）。
- 详情页保留「✏️ 编辑书籍」入口。

### 5. 单本「正在读」约束
- 保存书籍时若要把第二本设为「正在读」，提示「请先完成《当前书名》再开始新书」并阻止保存。
- 把某本「想读」书通过打卡变成「正在读」时，会**自动把之前那本正在读降级回「想读」**（进度保留），保证任意时刻仅一本正在读。

### 6. 徽章系统
- 独立「🏅 徽章」底部 Tab，展示全部 18 枚徽章（已解锁高亮、未解锁置灰 + 🔒）。
- 首页徽章区只展示**最近 2 枚**，超出部分用「**+N**」一键跳转徽章页（按真实首次解锁先后排序）。

### 7. 成长地图 / 计时 / 评论
- 成长地图扩到 **12 个节点**（含声音收藏站、阅读达人到阅读宗师等里程碑），节点详情弹窗显示进度百分比。
- 阅读计时器 **1 分钟步进**；秒表 `MM:SS` 实时显示，结束时按真实秒数自动取整赋值分钟数。
- 书籍支持「📝 读书笔记 / 评论」，在书架卡片与详情页可见。

---

## 早期已落地的基础能力（背景）

- 首页：档案、等级进度、每日目标、今日打卡；移动端优先的圆角卡片 + 柔和渐变 + 星星/云朵/书本视觉。
- 书架：想读 / 正在读 / 已读完 三态 CRUD；点击封面首字母生成彩色书脊。
- 打卡：5/10/20/30/60 分钟快捷 + 手动输入 + 备注 + 语音小记（≤60s，仅存本机）。
- 报告：周/月统计 + 最近 7 天趋势（按周一~周日排序）。
- 资料：自定义昵称 / 头像（emoji 或本地照片，256×256 裁剪，家庭共享同步）/ 生日按「X岁X个月」计算。
- PWA 可安装、可离线；Supabase 可选家庭共享（实时同步、刷新回环已修复）。
- 报告页设置卡片含「💾 下载本应用 HTML」「🗑️ 清空全部数据」。

---

## 数据模型要点（便于二次开发与排错）

```
state = {
  child:    { name, age, avatar, photo, birthday, setup, updatedAt, earned:[] },  // updatedAt=资料冲突时间戳
  settings: { familyCode },                                            // 家庭共享码，null=未共享
  books:    [ { id, title, author, category, status, note,
                totalPages, lastPage, finishedAt, updatedAt } ],       // updatedAt=书籍冲突时间戳
  checkins: [ { id, date, minutes, points, note, audio,
                ts, bookId, pages, pageEnd, updatedAt } ],              // updatedAt=打卡冲突时间戳
  deleted:  { books:{id:ts}, checkins:{id:ts} }                        // 删除墓碑：id->删除时间戳，随同步传播
}
```

- 存储：`rgm_db_v2`（IndexedDB，store `kv`+`audio`）为主，localStorage 兜底。
- 云端：`reading_state` 表，按 `family_id` upsert；语音不上传，仅本机保留。

---

## 部署到 GitHub Pages（快速复看）

1. 解压本包，取 `index.html`。
2. 放到仓库根目录（或 `docs/`）并提交推送。
3. 仓库 Settings → Pages 选择对应分支/目录。
4. 首次进入在「设置」里填写昵称/头像；家庭共享选「创建/加入家庭」拿到 `familyCode` 即可多设备同步。

> 说明：本包为离线单文件应用，`index.html` 与 `儿童阅读成长地图.html` 内容完全一致，前者为 Pages 部署文件名，后者为本地编辑/备份文件名。
